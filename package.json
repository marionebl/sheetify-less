{
  "name": "sheetify-less",
  "version": "1.0.0",
  "description": "less plugin for Sheetify",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test/index.js"
  },
  "repository": "marionebl/sheetify-less",
  "keywords": [
    "less",
    "sheetify",
    "transform",
    "plugin"
  ],
  "license": "MIT",
  "dependencies": {
    "less": "^2.7.1",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "bl": "^1.1.2",
    "browserify": "^13.0.0",
    "css-extract": "^1.0.0",
    "dependency-check": "^2.5.1",
    "insert-css": "^0.2.0",
    "istanbul": "^0.4.2",
    "sheetify": "^5.0.0",
    "standard": "^8.0.0",
    "tape": "^4.5.0"
  }
}
